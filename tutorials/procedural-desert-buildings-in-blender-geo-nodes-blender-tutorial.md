---
title: Procedural Desert Buildings in Blender | Geo Nodes Blender Tutorial
source: YouTube
url: https://www.youtube.com/watch?v=HMxZTPjFoc0
author: Cinematic Cookie
ingested: 2026-06-25
blender_version: "[PENDING]"
tags: []
extraction_status: pending
frames_dir: tutorials/frames/procedural-desert-buildings-in-blender-geo-nodes-blender-tutorial/
frame_count: 0
---

# Procedural Desert Buildings in Blender | Geo Nodes Blender Tutorial

**Source:** [YouTube](https://www.youtube.com/watch?v=HMxZTPjFoc0)
**Author:** Cinematic Cookie
**Duration:** 21m40s | 1 section(s)

---

## Raw Data (for Claude Code extraction)


### Full Content [0:00]
**Transcript:** Kind: captions Language: en Okay, so today I'm going to show you how I made these procedural buildings in Blender. I prepared this this file here with these assets. These are just basic normal sets with textures and so on. So, let's start making our building mesh. I'm going to add a cube. Let's make something like this. Extrude. Take this shape here. Um yeah, this is going to be our base mesh for our building. Let's open up geometry nodes, new geometry nodes. The first thing we want to do is to create this inseted shape at the top. So, first we want to select all of the faces at the top. Normal, let's take the normal, separate the normal by its XYZ components, and let's preview this. Let's take the C value, and I'm going to set this to face. And I'm also going to turn on the viewer node color opacity and attribute text. Now you can see the C value selects all of the top faces, so we can use a compare node and compare it to a value of greater than 0.5. This will make sure to select only the top faces. Then we can use the inset node. This inset node is not default in Blender. I created this node and made a tutorial about it. You can also get this inset node for free. The link is in the description. So, we can use this as a selection, and now we can see what it does. First of all, we want to only not do it for the individual ones. And now we can see this creates this inset effect. Now we can plug that this into our output. Use a extrude node and let's use the top as the selection. Now we can extrude our inserted face. And let's Yeah, set it to slightly in set it and extrude it inwards. And now you can see this will automatically adapt. Um, we also want to use not use individual of the extruded mesh. So this is the effect we want. Now we can scatter the doors at the bottom. So first of all we want to delete the faces at the bottom or select the faces at the bottom. So this time we're going to do the same thing as for the top but we're going to select a value of below negative point five. So less than minus point five. This will select the faces at the bottom and yeah, let's store this value on a separate face or on a separate [music] branch of the geometry input and yeah, we're going to store this on the edges and now we can delete these edges or just these faces at the bottom which will now give us the only these faces here. So we can separate the edges and now we only have the edges from the bottom. So the reason why we did this did it this way is because now we have only these faces on which from which we can capture the normal which will be which we can use later for the rotation of the doors because if we didn't delete those these edges would have also this direction. But now since they don't have these bottom faces, they're actually only able to point in this direction. Let's turn these edges into points. So let's add a mesh to point. Set this to edges. Now we can use a instance on points and we're going to use our door collection. Plug that into our instances. Let's select separate reset children and relative and let's also check pick instance. Now we can use a align rotation to vector. So we're going to use our normal into the vector. Use that as the rotation. Let's set this to Which value do we need? The rotation is not working correctly because we first want to split those edges because now these edges kind of have also the normal from the sides here. So we can use a split edges here. Now, yeah, you can see what it does. It correctly gives us the rotation. So we're going to set the rotation to Y. So it's pointing away from the faces and now we can join this together with our inserted faces. Now let's scatter some windows. So we're going to take another branch of the input. Use a distribute points on faces. Let's turn on change it to Poisson disk. Change the minimum distance, let's say to 1 m. And what we can also do is we can only select the faces at at sides. So let's duplicate our selection from before. So, we only select faces from the bottom, duplicate this, and select the faces at the top. Five, let's combine these two with a Boolean math or. Now, we got these selections, and now we can just use another Boolean Boolean math and select everything that is not these two faces. Now, we got our correct collection, and now only points will be scattered at these sides here. Now, let's use a instance on points. This time we're going to use our window windows here. Plug that in there, pick instance, relative, separate children, reset children. Let's use the rotation from our distribute node. Now, the rotation is wrong. So, we can use a rotate rotation node, set it to local, and fix it. I think what we need to do is minus 90 on the X axis and 180° on the Y. Now, they are facing in the right direction, and now we can combine these with our doors. So, there are a few problems right now. First of all, these windows are way too close to the edge to the edge just which will, yeah, cause this, and they're also clipping into the doors. So, let's fix the edges first. Let's take the group input, and we want to keep the windows away from all of these edges. So, we can use a edge angle node, and select everything with every edge with a angle greater than .5. Let's separate all of these edges. So, now we can use this in a geometry proximity node. Set this to edges, and now we can measure the distance of all of these points, how far they are away from these edges here. So, select distance and find every point that is less has a little distance of less than .5. This will select everything that is close to the corners, and now we can just delete all of these points. And yeah, now you can control how far these have to be away from these corners with this compare node. So, let's fix the doors. So, we don't want to scatter windows that they are clipping into the doors. So, the way we can do that is get a bounding box of these doors. Let's make a little bit more space here. Bounding box and now we can do the same thing. We can use a geometry proximity node. Let's set this to face. And measure how far these points are away from the doors. Um this is not working. Everything is zero because this only this doesn't work on instances. So, what we have to do is use realize instances before we feed it into the geometry proximity node. Now it's treated as a real mesh, and now we can do the same procedure as before. Let's use a less than node and select everything that is closer than that. Let's add these together. Let's use a Boolean math or and combine those. And now you can see everything around the doors disappears. Great. So, now the windows, as you can see, there should be holes in the walls where the windows are. So, let's create these with a mesh Boolean into the original mesh. Let's take Let's combine the the doors and the windows together and let's use a bounding box. So, everything is a bounding box. Now we can use this bounding box as a Boolean on this mesh here. So, let's use a mesh Boolean. And let's plug the bounding boxes into the mesh too. And yeah, you can set this to exact or float. Now, I want these a little bit these holes a little bit thicker, so let's use a scale instances. Scale instances node and before we feed it into the bounding box, we can scale those a little bit on the Y axis, so the bounding box becomes bigger and these holes get a little bit deeper. I don't want these doors to be right on the ground here. I want them slightly above, so we can use a Let's make a little bit more space here. Use a translate instances before and just move these doors up a little bit on the Z value. Now we got these booleans. Now, we want to delete the interfaces. So, we want to have actual holes in there. So, um how do we do that with a mesh boolean? If we capture a attribute before the mesh boolean. And let's just use any kind of value. Let's capture the faces. You can see we got the selection, but so we basically could invert these. Or use a separate node, set it to faces, and now all of these are deleted, but we don't want to delete everything that is affected by the boolean. We want only the inner faces. We want to keep those. If we look at the selection, we real- we realize that all of these faces at the side here are connected to a point which is a value of one. What we can do is first force it to calculate the selection on points. And then on faces again. So, now we can see all of these faces are exactly zero, and all of the others have a value of above zero. So, we can use a compare node greater than zero, and now we can or we can actually use a equal to zero, and now we can delete all of the faces that are equal to zero. And now, we got the desired result. And now, you can see we actually can look inside those windows here. Now, what we also can do is inset these doors a little bit on the Y value, so they are kind of um merged into the wall a little bit. And we can do the same thing for the windows. Only do that on the Y axis so they look a little bit integrated into these walls. Great. So, let's scatter some assets on the roofs. For that, we can simply get our extruded mesh top vertices vertices top faces distribute points distribute points on faces instance on time roof assets same procedures every time. Let's decrease the density to 0.1 or Yeah, like this. Now, we can just merge that together with the rest of our stuff and yeah, that's basically it. We can randomize the rotation a little bit with a random value. So, let's add another random value set it to vector. Use that as a rotation and let's only randomize it on the Z value so set X and Y to zero. Now, you can randomize it like this. Now, you have a seed for it. We can also use another random value for the scale. Let's set the minimum scale to point eight and the maximum scale to point 1.2 and now we have a little bit of random scale going on. Um yeah, these are also clipping outside so we can do the same thing as before again. We can use these edges. For example, this face you can see we have a edge right in the middle and we don't want to delete points that are the assets that are in the middle. We want to keep those. We only want the actual outside of the roof. So, we can use a edge neighbors node and select every edge that has less neighbors than two. Oops, less than and this will leave us with only the outside edges as you can see. Now, we can separate those. Use the proximity again, geometry proximity. Set it to address. Delete everything with a distance of less than meter. Yeah, point point five should should be good. And now you can see everything that is too close to the edges will be deleted. This window is a little bit too close to the top, so we can increase the minimum distance here. Let's set it to point six and this will delete the faces, the windows that are too close to it. So, yeah, these are basically some of the basic techniques I used for this desert generator. Yeah, let's let's add a material to this building. Go to shader editor, create a new material. Let's search for some concrete textures. So, you can drag and drop that in here. Let's set it to object box project. Let's scale that up a little bit. Let's set this to Let's colorize this with a color ramp. Make it slightly yellowish like this. Let's plug that into uh the base color and Yeah, &gt;&gt; [music] &gt;&gt; this looks good. Now, we can use this color also in the height of a bump node. You set it to normal. Yeah, we can also mix this together with another with another concrete texture. Let's use this one. Also, use box fret. And just mix this into the color, so we get a little bit more if a randomized result. And we can also use a bevel on these edges, so these look a little bit less sharp. Yeah, what you could also do is create a separate material for the roof. So, let's call this one roof. And use a separate material for only the extruded top, so we can use a set material. Use the top as a selection, and use our roof. And use this for another texture. Let's just take this one. Box project. Object. Color ramp. Let's make this a little bit darker. Scale this up. Another thing I wanted to show you is how to scatter these dents along the walls. So, for that we can simply use um these edges we selected from our doors. So, let's create a separate branch from them. And let's convert those to a curve. So, mesh to curve. So, the reason we did that is because now we can use a curve to points node. Yeah, the wrong one. Points. Curve to points. Um now we can set this to length and let's set the length to 1.5 m. Now we can use a instance on points again and we'll use our What are these called? Wall tense collection. Use that as the instance. Pick instances, relative, separate, reset children. And now for the rotation, we can basically Let's make some more space here. Use the same rotation as for the doors. So, let's use a copy this, align rotation to vector, use that as a vector. And now we can also just No. Plug this in with the rest of our stuff. And now to avoid these overlapping ones, we can just trim the curve a little bit before we use them as the points. So, can trim the start and the end to get something like this. Um yeah, what you could also do is just use a random value, set it to Boolean, and use this as the selection for the instances of on points. And now you can control how many there will be with this one. Yeah, and now we have our procedural building that will adapt to any shape we give it to it. If you want to increase the performance while working, you can just simply turn off the Boolean mesh for while you're editing cuz now everything is running a lot faster than before. And yeah, once you're already ready to render, you can just turn it off and turn it on again. So, this is how I made these procedural buildings. The full version of this generator is available on the Blender Market with a few more features and the ability to export it with generated UVs. Combined with my nature generator, you can quickly generate cool scenes. I was able to create all of these shots here in about 3 hours. Check out my channel for more Blender videos or watch some of my short films.



---

## Structured Notes

### Core Technique
[PENDING EXTRACTION]

### Summary
[PENDING EXTRACTION]

### Key Steps
[PENDING EXTRACTION]

### Nodes / Settings
[PENDING EXTRACTION]

### Difficulty
[PENDING EXTRACTION]

### Blender Version
[PENDING EXTRACTION]

### Tags
[PENDING EXTRACTION]

---

## Related Tutorials
[PENDING EXTRACTION]
