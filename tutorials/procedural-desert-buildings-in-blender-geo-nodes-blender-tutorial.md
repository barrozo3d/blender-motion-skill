---
title: Procedural Desert Buildings in Blender | Geo Nodes Blender Tutorial
source: YouTube
url: https://www.youtube.com/watch?v=HMxZTPjFoc0
author: Cinematic Cookie
ingested: 2026-06-25
blender_version: "[PENDING]"
tags: []
extraction_status: pending
frames_dir: tutorials/frames/procedural-desert-buildings-in-blender-geo-nodes-blender-tutorial/
frame_count: 0
---

# Procedural Desert Buildings in Blender | Geo Nodes Blender Tutorial

**Source:** [YouTube](https://www.youtube.com/watch?v=HMxZTPjFoc0)
**Author:** Cinematic Cookie
**Duration:** 21m40s | 1 section(s)

---

## Raw Data (for Claude Code extraction)


### Full Content [0:00]
**Transcript:** Okay, so today I'm going to show you how I made these procedural buildings in Blender. I prepared this file here with these assets. These are just basic normal sets with textures and so on. So let's start making our building mesh. I'm going to add a cube that makes something like this extrude. Take this shape here. Yeah, this is going to be our base mesh for our building. Let's open up geometry nodes, new geometry nodes. The first thing we want to do is to create this insetted shape at the top. So first we want to select all of the faces at the top. Let's take the normal, separate the normal by its x, y, c components and let's preview this. Let's take the c value and I'm going to set this to face. I'm also going to turn on the viewer node color opacity and attribute text. Now you can see the c value selects all of the top faces. So we can use a compare node and compare to a value of greater than 0.5. So we'll make sure to select only the top faces. Then we can use the inset node. This inset node is not default in Blender. I created this node and made a tutorial about it. You can also get this inset node for free. The link is in the description. So we can use this as a selection and now we can see what it does. First of all we want to only not do it for the individual ones. Now we can see this creates this inset effect. Now we can plug that into our output. Use a extrude node and let's use the top as a selection. Now we can extrude our insetted face and let's set it to slightly insetted. Extrude it inwards. Now you can see this will automatically adapt. We also want to use not use individual of the extruded mesh. So this is the effect we want. Now we can scatter the doors at the bottom. So first of all we want to delete the faces at the bottom or select the faces at the bottom. So this time we are going to do the same thing as for the top. But we are going to select a value of below negative 0.5. So let's minus 0.5. Select the faces at the bottom and let's store this value on a separate face or on a separate branch of the geometry input. And we are going to store this on the edges. Now we can delete these edges or those faces at the bottom. Which will now give us the only these faces here. So we can separate the edges and now we only have the edges from the bottom. So the reason why we did this way is because now we have only these faces on which we can capture the normal. Which we can use later for the rotation of the doors. Because if we didn't delete these edges would have also this direction. But now since they don't have these bottom faces, they are actually only able to point in this direction. Let's turn these edges into points. So let's add a mesh to point, set this to edges. Now we can use an instance on points and we are going to use our door collection. Plug it into our instances and let's select separate research children relative. And let's also check pick instance. Now we can use a align rotation to vector. So we are going to use our normal into the vector. Use that as a rotation that set us to which value do we need. Now the rotation is not working correctly because we first want to split those edges. Because now these edges have also the normal from the sides here. So we can use a split edges here. Now you can see what it does. It correctly gives us the rotation. So we are going to set the rotation to Y. So it's pointing away from the faces. And now we can join this together with our inserted faces. Now let's scatter some windows. So we are going to take another branch of the input. Use a distribute points on faces. Turn on, change it to poison disk. Change the minimum distance. Let's say to one meter. And what we can also do is we can only select the faces at the sides. So let's duplicate our selection from before. So we only select faces from the bottom.plicate this and select the faces at the top five. Let's combine these two with a boolean math or now we get these selections. And now we can just use an adult boolean math and select everything that is not these two faces. Now we got our correct collection. And now only points will be scattered at these sides here. Now let's use an instance on points. The time we are going to use our window. Windows here. Put it in there. Pick instance relative, separate challenge, research, children. Let's use the rotation from our distribute node. Now the rotation is wrong. So we can use a rotate rotation. Note, set it to local and fix it. Think what we need to do is minus 90 on the x-axis and 180 degrees on the y. Now they are facing in the right direction. And now we can combine these with our doors. So there are a few problems right now. First of all, these windows are way too close to the edges, which will cause this. And they are also clipping into the doors. So let's fix the edges first. Let's take a group input. And we want to keep the windows away from all of these edges. So we can use an edge angle mode. And select everything with every edge with an angle greater than 0.5. Let's separate all of these edges. So now we can use this in a geometry proximity node. Set this to edges. And now we can measure the distance of all of these points. How far they are away from these edges here? So set the distance and find every point that is less, has a little distance of less than 0.5. This will select everything that is close to corners. And now we can just delete all of these points. And yeah, now you can control how far these have to be away from these corners with this compared node. So let's fix the doors. So we don't want to scatter windows that are clipping into the doors. So the way we can do that is get a bounding box of these doors. Let's make a little bit more space here. And now we can do the same thing. We can use a geometry proximity node. Let's set this to face. And measure how far these points are away from the doors. Does this not work? Everything is 0 because this doesn't work on instances. So what we have to do is use realized instances before we feed it into the geometry proximity node. Now it's treated as a real mesh. And now we can do the same procedure as before. Let's use a less than node. And select everything that is closer than that. Let's add these together. Let's use a boolean math or... ...and combine those. And now you can see everything around the doors. This appears great. So now the windows, as you can see, there should be holes in the walls where the windows are. So let's create these with a mesh boolean into the original mesh. Let's take... Let's combine the doors... ...and the windows together. And let's use a... ...bounding box. Everything is a bounding box. Let's use this bounding box as a boolean on this mesh here. So let's use a mesh boolean. And let's plug the bounding boxes into the mesh too. And yeah, you can set this to exact or float. Now I want these... ...holds a little bit thicker. Use a... ...scale instances. Scale instances note. And before we feed it into the bounding box, we can scale those a little bit on the Y-axis. So the bounding box becomes bigger. And these holes get a little bit deeper. I don't want these doors to be right on the ground here. I want them slightly above, so we can... ...use a... ...let's make a little bit more space here. Use a translate... ...instances before and just move these doors up a little bit on the set value. Now we got these booleans. Now we want to delete the interfaces. So we want to... ...have actual holes in there. So how do we do that with a mesh boolean? If we capture a attribute... ...before the mesh boolean. And let's just... ...use any kind of value. Let's capture the faces. You can see we got the selection mode. So we basically could invert these... ...or use a separate node set to faces. And now all of these are deleted. But we don't want to delete everything that is affected by the boolean. We want only the inner faces. We want to keep those. If we look at the selection, we realize that all of these faces at the side here are... ...connected to a point which is a value of 1. What we can do is first force it to calculate the selection on points... ...and then on faces again. So now we can see all of these faces are exactly 0. And all of the others have a value of above 0. So we can use a compare node... ...greater than 0. And now we can... ...or we can actually use a equal to 0. And now we can delete all of the faces that are equal to 0. And now we got the desired result. And now you can see we actually can look inside those windows here. Now what we also can do is... ...inset these doors a little bit... ...on the Y-AG value. So they are kind of merged into the wall a little bit. And we can do the same thing for the windows. Let's only do that on the Y-Axis. So they look a little bit integrated into these walls. Great. So let's scatter some assets on the roofs. For that we can simply get our exited mesh top radius... ...vorities top faces... ...just to the points... ...just to the points on faces... ...and then... ...continues roof assets, same procedures every time... ...let's decrease the density to 0.1... ...or... ...like this. Now we can just merge that together with the rest of our stuff. And yeah, that's... ...basically it. And we can... ...randomize... ...the rotation a little bit with random values. So let's add an random value, set it to vector... ...use that as a rotation. And let's only... ...randomize it on the set value. So set X and Y to 0. Now you can randomize it like this. Now you have a Z for it. We can also... ...use another random value... ...for the scale. Let's set the minimum scale to 0.8... ...and I'm going to make some scale to 0.1.2... ...and now we have a little bit of random scale going on. Yeah, these are also clipping outside. So we can do the same thing as before again. We can... ...use these edges. For example, this face you can see... ...we have edge right in the middle... ...and we don't want to delete points that are... ...to assets that are in the middle. We want to keep those. We only want the actual... ...outside of the roof. So we can use a edge neighbors node... ...and select every edge that has... ...less neighbors than two. Oops, less than. And this will leave us with only the outside edges... ...as you can see. Now we can separate those... ...use the proximity again... ...dermetry proximity. Set it to edges. Delete everything with... ...the distance of less than... ...meter, yeah, 0.5. That should be good. And now you can see everything that is too close to the edges... ...will be deleted. The window is a little bit too close to the top... ...so we can increase... ...the minimum distance here. Let's set it to 0.6. And as well delete the faces... ...the windows that are too close to it. So yeah, these are basically some of the basic techniques... ...are used for this desert generator. Yeah, let's add a material... ...to this building. Go to Shady Editor, create a new material. Let's search for some... ...concrete textures. So we can drop that in here. Let's set it to object, box project. Let's scale it up a little bit. Let's add those to... ...color-rises with a color ramp. Make it slightly yellowish... ...like this. Let's plug it into all the base color. And... Yeah, this looks good. Now we can use this... ...color also in the height of a bump node. You set it in normal. Yeah, we can also mix this together... ...with another... ...with another concrete texture. Let's use this one. Also use box project. And just... ...mix this into the color. So we get a little bit more... ...if a randomized result. And we can also use a... ...babel on these edges. So these look a little bit less sharp. Yeah, what you could also do is... ...create a separate material... ...for the roof. So let's call this one roof. And use a... ...s separate material... ...for only the extruded top. So we can use a set... ...material. Use the top as a selection... ...and use our roof. And use this for another texture. Let's just take this one. Box project... ...object. Color ramp. Let's make this a little bit darker. Scale this up. Another thing I wanted to show you is how to... ...scatter these tents along the walls. So for that we can simply use... ...these edges. We selected from our doors. So let's create a separate branch from them. And let's convert those to a... ...curve. So mesh to curve. So the reason we did that is... ...because now we can use a... ...curve to points node. Yeah, the wrong one. Points. Curve the points. Now we can set this to length. And let's set the length to 1.5 meters. Now we can use a instance on points again... ...and use our... ...what are these called wall tents collection. Use that as an instance. Pick instances, relative separate research children. And now for the rotation. We can basically... ...let's make some more space here. Use the same rotation as for the doors. So let's use a... ...copy this... ...to vector. Use that as a vector. And now we can also just... ...no... ...plug this in with the rest of our stuff. And now to avoid these overlapping ones... ...we can... ...just trim the curve a little bit before we use them as the points. So we can trim... ...the start and the end. To get something like this. Yeah, what you could also do is just use a random value... ...set it to boolean... ...and use this as a selection. For instance, it's on points. And now you can control how many there will be with this one. Yeah, and now we have our procedural building... ...that will adapt to any shape we give it to it. If you want to increase the performance while working... ...you can just simply turn off the boolean mesh... ...follow while you're editing. Because now everything is running a lot faster than before. And yeah, once you're ready to render... ...you can just turn it off, turn it on again. So, this is how I made these procedural buildings. The full version of this generator is available on the Blender Market... ...with a few more features and the ability to export it... ...with Generated UVs. Combined with my nature generator... ...you can quickly generate cool scenes. I was able to create all of these shots here in about 3 hours. Check out my channel for more Blender videos... ...or watch some of my short films.



---

## Structured Notes

### Core Technique
[PENDING EXTRACTION]

### Summary
[PENDING EXTRACTION]

### Key Steps
[PENDING EXTRACTION]

### Nodes / Settings
[PENDING EXTRACTION]

### Difficulty
[PENDING EXTRACTION]

### Blender Version
[PENDING EXTRACTION]

### Tags
[PENDING EXTRACTION]

---

## Related Tutorials
[PENDING EXTRACTION]
