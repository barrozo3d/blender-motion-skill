---
title: 3D Smoke (Blender Geometry Nodes)
source: YouTube
url: https://www.youtube.com/watch?v=Vqe4jBf3wx4
author: Seanterelle
ingested: 2026-06-23
blender_version: "[PENDING]"
tags: []
extraction_status: pending
frames_dir: tutorials/frames/3d-smoke-blender-geometry-nodes/
frame_count: 4
---

# 3D Smoke (Blender Geometry Nodes)

**Source:** [YouTube](https://www.youtube.com/watch?v=Vqe4jBf3wx4)
**Author:** Seanterelle
**Duration:** 34m28s | 1 section(s)

---

## Raw Data (for Claude Code extraction)


### Full Content [0:00]
**Transcript:** Hello everybody and welcome to this geometry notes tutorial in Blender 5.0 using the new volume grid nodes, new ish, it's been a while since they came out actually. But nonetheless, we're going to be making this 3D smoke with geometry nodes. So, let's get into it. The basic structure of this whole thing is that we're going to have a simulation zone here, represented by this cube, and we've got an emitter, which is going to be Suzanne. They've got their own geometry nodes networks that I'll explain. Basically, we're going to set up a domain, which is going to be this cube, just subdivided into a bunch of little voxel cubes. Then, we are going to do something really similar to the 2D fluid simulation tutorial that I had a long time ago, where we're using a couple different fields, velocity divergence, pressure, and then a density field which we're actually calling smoke. Here, that smoke field is what you actually see. Then, we're essentially creating forces, enforcing incompressibility in the fluid, and watching the density or the smoke evolve over time. So, that's our basic setup. We have our initialization here, we have our simulation here, and then a little bit of post-processing, just removing grids, making the voxel data structure slightly more efficient before we bake, so that our baked output can be as small in memory as possible. So, first things first is our domain setup. First thing we're going to do is create these simulation grids. We're going to do that with the volume cube node, just to create an initial volume. We're going to use the inputs from our parameters node group, which is also right out here. So, the reason we create a node group like this is just so that we can reference these sort of global values that we want in multiple places without having to drag a bunch of noodles around. So, we create that volume cube with the min and max bounds that are defined in our parameters node group. In this case, I think it's just negative 2 to positive 2. And then also using the solver resolution, you can press control h to hide or show all the different outputs of a specific node group. That's why these are actually the same, but they look different. And so, the solver resolution, we're going to set, this is just something, relatively small, like 64 in this case, so that if I do turn off the baked output with m for just masking that, then we can still run this relatively quickly. If we go down to something like 32, it'll run faster. But anyway, 64 is fine. And then the cool thing about that is that if I simulate the solver resolution at 64, what we're going to do later is we're going to use the smoke resolution at a higher resolution so that you get more detail and you don't see all these little blocky cubes here. But the cool thing is I can run at this relatively interactive rate where, you know, when it bakes, I can see it running as fast as it will in the final render. And then, that will give me a really good idea of what the higher resolution output will look like. And I can illustrate that by just unmasking the bake node. And you can see that the shapes match up pretty much perfectly. It's just in the 64 version where the smoke resolution is also 64. It looks more blurry. So, working this way will give you an idea of what your effect will actually look like without taking forever to bake, because it takes a lot longer when you bump up. The smoke resolution is something like 256, like 128 or 256. Okay, so create a volume cube. We get the density, which is the default grid value or grid attribute, whatever you want to call it, that it creates. We're going to remove that because we don't need it. We're actually going to call it smoke. We're going to voxelize the grid. And the reason why we're doing this is that the underlying data structure for these volume grids is called openVDB. And it will do some memory optimization where contiguous blocks of voxels that are adjacent to each other that have really similar values will be just referenced as one thing with their own individual bounds. And that's all well and good. But for what we're doing, we want all of the voxels available so that we can calculate the divergence and pressure and enforce incompressibility in the way that we want. And we just don't want any of that optimization right away. We're doing the simulation. And then we'll use the topology from that voxelized grid to initialize all of the other grids that we need for our simulation. So we've got velocity, divergence, and pressure. Velocity is a vector grid, the other two are float grids. So we just use this field to grid node, which will take in the topology input, which will define what does the grid that you're creating actually look like in terms of its bounds, like its minimum and maximum points, as well as the topology of the voxels. So in this case, it's just a bunch of voxels at the resolution that we set here. But in other cases, it could be optimized voxels apologies, like we just talked about. So now we have all of our grids in the same topology. And then we're going to add our smoke grid. So the way we do that is we're the reason we're doing this separately is because we're going to want it to be a different resolution. So here, press control H, instead of having solver resolution, we smoke resolution. And right now, I've just got it set to 64 so that we have like an interactive frame rate. But if I said it's something, something higher, like 128, and it's going to take longer to simulate here. But you can see how you get a little bit more detail. And so you can just ramp that up to get more and more detailed smoke with the same solver resolution, which is really cool because then you can, like I said, get that really good idea of what your simulation is going to look like before you actually spend the time to bake out a whole high resolution output. So yeah, you can see, you know, that looks a bit better than the 64 version. But right now, we'll set to 64. Ultimately, the final render will actually set that to 256, which is quite high. And it will take a long time to bake. So we'll do the same thing. We'll get the density grid, we'll remove it, we'll voxelize it, we'll multiply it by zero just so that it smokes starts at zero. Because it's a flow grid and we don't want any smoke to start out with, we'll add that during the simulation. And then we are done with the initialization. Now, before we jump into the actual simulation loop, I want to talk about the emitter. So we're using Suzanne for the emitter here. And really all we're doing here is we are taking the geometry, we're moving it in this like random way. Here, I'll show you without the simulation on. So we're just moving Suzanne around in this kind of random way driven by these two noise textures, one for the position and one for the rotation. And then we're using this simulation loop to store the last position and new position so we can get the differential in our simulation node group so that we can use that velocity to drive the forces that the smoke that gets emitted by the emitter initially have. So as Suzanne moves around, she kind of pushes the smoke and it plumes out in a cool way. It's just what I like. And so you can do whatever you want. But the details here of how this kind of works is we are subdividing the surface because I haven't done that before. So yeah. And then we store the original position of Suzanne right here and just put that into the simulation loop. And the reason why we're doing that is we don't want to like iteratively apply the randomized transformations because then Suzanne kind of might like float away out into the sky or down below the sea. And instead we just we kind of wanted to act like there was no simulation group but we need the simulation group so that we can iteratively through each frame store the new position and the last position. Okay so then each frame we take that original position and then we transform it by this which just takes these two noise textures that are driven by the scene frame and tuned a bit and scaled. So this is all just tuning and then we convert this bottom one to rotation because that controls the rotation. This is translation. We can find that into a transform transform the original point. And before we transform the actual Suzanne geometry by that randomized transformation where we'll store the position as an attribute called last position and then we transform it and then we store that new transformed position as the new position attribute. And that's it then we're just going to set shade smooth and set the material for our Suzanne and that'll be for rendering. Okay so turning back our simulation group on. The first part of our simulation is adding smoke. So what we're going to do is we'll take in our emitter geometry and then we're going to just add smoke near it. So we're going to sample the the nearest point for each voxel grid and if the did we're going to sample the position of the nearest point and if the distance from the position of our current voxel position and that position is less than this amount then we're going to add a certain amount and scale that just by two to the smoke that we already have. And so here we get the name grid smoke and then you can just like use the math nodes on that to add a little bit to that grid based off of the position of the voxel then you just store that again with a store named grid in the same name. So smoke and then we've got our addition of smoke to the simulation. After that we're going to add velocity and that's where what we did in the emitter node group comes in the play. So basically it's the same sort of thing. We're going to apply velocity to the velocity field that will affect the smoke near the surface of Suzanne based on the new position and last position that we stored in the emitter node group. So working backwards we'll start with getting the velocity grid then we're going to add a little bit of velocity to that based off of distance from Suzanne and the new position and last position and then store that back into the velocity grid. So to make sure that the velocity that we're adding is only near Suzanne we're going to scale whatever velocity we're adding by the distance of the voxel position from the closest new position of Suzanne. So the new position is the position this frame because that's how we started before. So that's the same as the last node group and we're doing less than a different value so 0.025. I'm just tuning that to my liking and then multiplying by 2.5 again that's a scaling factor that you can tune as you like. So for the actual velocity that we want to add near Suzanne's surface what we'll do is we'll take we're sampling both the new position and last position from the closest point on the surface to the voxel grid and then we're subtracting we're subtracting the new position we're taking the new position minus last position. So subtracting the last position from the new position and that got a vector from the last position to the new position and error pointing there. We scale that by 0.5 because this is again a tuning parameter and then we're adding a little bit in the z direction just so that the smoke kind of floats up or it starts by going up and that's also just something that I like you could totally take that away if you wanted. Again then we're scaling that velocity by the proximity to the emitter and then we're adding that to the original velocity or the existing velocity field and there you go that's how we add velocity. So the next step is pretty important and it's infecting the velocity which makes the velocity carry itself through space over time. So the velocity is going to affect itself. So similar to before we're going to get a name get the velocity named grid and then we're going to store that after some transformations. And what's going to happen here is we're actually going to sample the velocity field at the current voxel index to get the velocity that currently exists and then we're going to subtract that velocity vector from the position of the current voxel and then sample the velocity field again at that offset point and then we're going to just set that as the velocity at the current point. So it's like okay if we're moving this way then we look back a little bit and we just bring the field to where we're currently at and that is the advection which just means moving a field through space over time by velocity. And so that's what we're doing. We sample the grid index at the voxel index so this is just the x, y, z index of our current voxel and we're sampling velocity there. So that's our velocity that we currently have at our current voxel and then we're subtracting that from the position of the current voxel and then we're sampling the velocity grid again at that new offset position and then we're using the field to grid node. I'm not I don't even know if we actually need this. We definitely do. That is because oh we need it because we have a field from the sampling of the grid. So we're not existing just in grid space. Jamshy nodes and the way they've implemented this does some implicit conversions but it doesn't it works if you're using like math nodes like if I just like took this grid and multiplied it by two or scaled it or something like that. But if you're working with fields at which you'll definitely get from doing the sample grid index or sample grid node then you'll have to convert that back to a grid using field to grid and we'll just use the same topology as velocity because we don't want it to change and then we will bring that offset velocity back into the current velocity and that's how you would vect velocity. There is an advect node like a volume of vect node but I find it doesn't work the way that I want it to and maybe I'm doing something wrong let me know if I am but I've done quite a bit of testing and it tends to be a little wonky. But anyway let's go on to calculating the divergence. Now so like what happens is after the velocity of evaction the fluid or the density might it well the fluid might compress or expand but real fluids are incompressible so we want to enforce incompressibility. So we want to calculate the divergence to know how far off we are from an incompressible state which would have zero divergence and so the way that we're going to do that is we're going to look at the six different neighbors so we're getting the well okay I'll do an overview first and then we'll see how it's implemented in the notes. So we're going to look at the six neighbors of the current voxel we're going to get the velocity at each point and then for each axis we're going to so x, y and z we're going to subtract that get the difference and then we're going to normalize by the the sort of like voxel size in this case we're just assuming the voxel size is one so divided by two because it spans two voxel spans and then we'll sort of that in the divergence. You could also just use the grid divergence node and in this case you can't assume that voxel size is just one because under the hood this will account for this actual size of the voxel depending on the bounds and so since our min and max bounds are negative two to two that is four units so what what we would do is we do four divided by the solver resolution and then multiply that by the divergence that comes out of this grid divergence node and then we would just like pop that in here instead of using this whole network. So I put that in there just so you can see how that might work it's a lot easier a lot simpler but in my testing it didn't look as good there was it just didn't keep all the detail I don't really know why so I'm just doing it the manual way and so the way that we do that is we use these get value at offset vector nodes so it's just getting the value at offset and then it's a vector output and so what we do is we take the current voxel index and then we add the offset that you input as parameters to the node group and then we sample the same grid at those offset indices and then you get the value of the grid that you input so in this case we're getting the value of the velocity at offset positions and we just go for x in the positive and negative direction and then positive and negative for both y and z and then for each of those we're subtracting the negative direction from or the negative offset direction from the positive offset direction now we're adding those all up and then we're dividing by two to normalize by the voxel size and then we use the field to grid with the topology from velocity and we're storing that back in the divergence so that's how you calculate the divergence because if you think about it it's like in the x direction like to just think in one dimension you're subtracting the amount coming in from the left side from the negative x offset position from the amount going out or yes going out from the positive direction so in the negative direction a positive value means negative divergence because it's coming in to the voxel and a positive value on the positive offset side means positive divergence so instead of adding them together we actually subtract the negative side because if it's negative then you want to add divergence because it's going away outside of the current voxel so that's why you subtract for each of them you just do that for each dimension and then you just need to divide by two because it normalizes for the span of the voxel sizes between the like positive offset and the negative offset and there you go that's how you calculate your divergence so now we have the divergence at each point but we need pressure and the reason we need pressure is because divergence is sort of like local to the actual voxel but pressure you calculate for the whole field and so we do this in an iterative way and it uses the divergence over a series of iterations in this case we're doing 16 to figure out how much pressure is coming in or out or like pulling or pushing compressing or decompressing from any given point in the voxel grid and then after we calculate the pressure we can get the because that's just a scalar value we can get the gradient of that which is a vector and then subtract that from the current velocity field to push the velocity field into a place where when you calculate the divergence you get zero which means the fluid is incompressible so subtracting the pressure gradient oh it's also called the projection step but subtracting the pressure gradient helps to enforce incompressibility so first to get the pressure gradient we need to calculate the pressure so we're going to do user repeat zone to do whatever iterations you want to do in this case we're just doing 16 and within each pressure iteration we're using another get value at offset node in this case it's just for a floating point which is why it doesn't have that vector suffix so again in each direction positive negative why positive negative z positive negative and so we're getting the pressure at each of those offset points this is the same setup as the previous node group then you add those altogether you convert to a grid and then you subtract the divergence from that and then you average so you divide by the total number of voxels that you're sampling the pressure from and so to gain some sort of intuition for why this works is that the adding of all of the neighboring voxels pressure and then ultimately averaging them sort of spreads out the pressure so you get this sort of like global influence to all the different voxels and then we subtract the divergence from that because the pressure will counteract the divergence to enforce incompressibility so if that divergence is positive we want the pressure to be negative we want to pull in and if the divergence is negative we want the pressure to be positive and push out so that when we ultimately subtract the pressure gradient in the direction that the pressure field is either increasing and we want to subtract that then if the divergence is positive it'll make it more negative and if it's negative it'll make it more positive and it'll push it towards that zero stable incompressible state and so after we do our iterations to try and converge on the actual pressure which we may or may or may not get to 100% but it's okay because we just care about it looking good in this situation and not being totally physically accurate we can move on to subtracting the pressure gradient so what we need to do here is we just need to get the pressure gradient and then subtract it from the velocity field so this is another situation in which there is a dedicated node for getting the grid gradient that you could use so I have it up here but again when I tested it it just didn't look as good like there was like little details like a little sharpness that was somehow lost so it's here if you want to try it but you can also do it the long way it's not that long but yeah you do the same thing where you do the span of the domain size divided by the resolution of the solver and then scale the grid gradient that you get out of the grid gradient node by that so you'd get the gradient of the pressure scaled by that and then this is your pressure gradient so the way that we calculate that here is we're going to essentially get the gradient in each dimension x, y and z and then combine those into a single vector so what you do is you get the value of offset which we've seen this node group before of the pressure at each positive and negative direction for each dimension like we've done before so you subtract the negative offset position from the positive one for each one and then that gets the difference of the pressure from positive negative in each dimension and then you just combine that into a vector so the x difference for x, y difference for y, z difference for z and then that's your pressure gradient so convert that to a grid because it's a field at this point and then we're just going to subtract that pressure gradient from the velocity field and store that back in the velocity and we have improved and tried to enforce incompressibility which is what we really need and then the last step, the very last step is infecting the smoke and so we've infected the velocity before and the basic principle is you just take the velocity at the given point and then you sample whatever field you're trying to infect at the position of the current voxel offset by the velocity of the current voxel so again there's like an advex grid node that you could use but it does weird things and I end up not using it but again I have it here just in case you want to use it the long way is also not very hard and the way that that works is we want the smoke grid because that's what we're infecting we also want velocity because you need the velocity to infect and you give them grid so we're going to sample the velocity grid at the position of the current smoke grid point and then we're going to subtract that from the position of the current smoke grid point and then sample the smoke grid at that offset position and then convert that from a field to a grid and then we're going to multiply by 0.96 just so that the just so it dissipates a little bit over time so the smoke kind of like fades away instead of just staying around forever and then you store that back into the smoke grid so this does a little bit more than just advexion it also does this fading but mostly it's smoke evaction and then that's it that is it for this node group I'm going to turn back on the baked version just so we can play it and the last thing that we do is just a little bit of saving or like pruning and baking and and some clean so let me go over that so the last thing we're doing is we're just setting the material so that we can control how it looks in the render we're removing grids so I'm removing the velocity the pressure and the divergence so all we have is the smoke and one reason for that is if I pop it a view or node right now what you're seeing is you're going to see the smoke grid but if I don't remove the grids you you might see something different okay the reason why it switched back is because it's I haven't pinned that view so you're not seeing the view but if I turn that back on you can now see that without removing the grids right here let me just duplicate this and then I will ungroup it and then I'll put this in here so we can just operate at this level out issues with deactivating this view or node so I got this view or node on if I remove all these then we get our velocity or our smoke grid and we can see it but if I don't then you might see something else so in this case you're seeing the pressure grid but like you can barely see it so you don't really see anything here but it is there and then same for velocity and everything else like I believe that this is you can see like some tiny green because like with a vector grid you'll see the xyz for red, green and blue but anyway so it'll get confusing if you have all these grids and you pop it in a view or node and you can't see anything or see what you want to see so just remove all of the different grids that you don't actually want to see this is also important for the baking step because what you don't want is to have a bunch of grids especially at like higher voxel resolutions that are getting baked and stored because that's a lot more memory that you're eating up or disc space so remove them for efficiency's sake as well as visualization so I'm just going to go ahead and delete those and then also we're going to prune smoke so that open vdb memory optimization that we talked about at the beginning comes into play here so the pruning the grid is just pruning it by a certain threshold which we set at .01 so if the value of this smoke grid is below .01 we prune that grid and then it gets optimized so you don't store it in memory and so we store that back into the smoke grid this won't affect the simulation now because we're outside of the simulation loop if we did this in the simulation loop then when you had vect it it might pull from like this one spot it gets weird um and so we just do this at the end and then we go directly into the bake node and this is where we can bake our high resolution output so that we can we can see what we see here in terms of the rendering if I shift alt-Z to turn off all of the overlays and we go Z and up this this is the render that we've got this is like what it's going to look like I'll just go to this frame 77 I like that frame we go to the shader editor you can see that the material for this smoke is just a principled volume um I have this color set to this purple color purple leapink lavender color you can set it to anything excuse me and then um yeah ooh that's nice I like that green oof man maybe I will play with it a bit here just to show you what you could do like let's set it to this green maybe little darker and then um for the emitter we also have our material that we set and are at the end of our geometry node's network so here this is just a metallic texture super simple with a reference to point one if we wanted to edit the color to work something with something here if you blueish green more blue than green more yellow uh maybe something like that and then we want to do the same thing for the background color uh maybe something like that and then darker darker later not too dark and not too light colors are always finicky man I mean I like that too you also do contrasting colors that looks terrible I mean that's kind of interesting but I'm gonna funky I'm gonna get too bright I don't know we don't want to have we don't want to spend so much time just messing around with this dewy like the brightness there and then let's go back to that frame yeah I don't know I mean you could see it I like having a darker background let's see if we darken that uh oh interesting I don't know okay well I'm not gonna bore you with this I'm like make this a video super long just because I'm playing with colors at the end but you get the idea um we're just gonna actually change this back to something like blue or green make it dark and we're gonna turn up this to be a bit lighter I want this yeah okay there you go that'll be that'll be pretty good um and then yeah so mess around with that change it however you want and I think that's it for this tutorial so I hope you enjoyed this tutorial I hope you learned something and above all I hope you had fun until next time bye

**Frame:** tutorials\frames\3d-smoke-blender-geometry-nodes\frame_000.jpg


---

## Structured Notes

### Core Technique
[PENDING EXTRACTION]

### Summary
[PENDING EXTRACTION]

### Key Steps
[PENDING EXTRACTION]

### Nodes / Settings
[PENDING EXTRACTION]

### Difficulty
[PENDING EXTRACTION]

### Blender Version
[PENDING EXTRACTION]

### Tags
[PENDING EXTRACTION]

---

## Related Tutorials
[PENDING EXTRACTION]
